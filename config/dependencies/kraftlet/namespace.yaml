apiVersion: v1
kind: Namespace
metadata:
  name: kraftlet
  labels:
    # Kraftlet runs hostNetwork (to reach the node-local ukpd loopback API
    # via the co-located TLS bridge), which requires the privileged Pod
    # Security level — the same posture as the ukp-system namespace.
    pod-security.kubernetes.io/enforce: privileged
